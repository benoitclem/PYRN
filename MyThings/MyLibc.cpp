
#include "MyLibc.h"

#include "mbed.h"

/*
char *strdup(const char *str) {
    uint32_t len;
    char *copy;

    len = strlen(str) + 1;
    if (!(copy = (char*)malloc(len)))
        return 0;
    strncpy(copy,str,len);
    return copy;
}
*/
